# 🤖 نموذج التنبؤ بالأسعار الفركتالي المتقدم
## Fractal-Based AI Price Prediction Model

---

## 📋 جدول المحتويات
1. [نظرة عامة](#نظرة-عامة)
2. [المكونات الأساسية](#المكونات-الأساسية)
3. [الصيغ الرياضية](#الصيغ-الرياضية)
4. [خوارزمية التنبؤ](#خوارزمية-التنبؤ)
5. [مثال عملي](#مثال-عملي)
6. [كيفية الاستخدام](#كيفية-الاستخدام)

---

## 🎯 نظرة عامة

هذا النموذج يجمع بين **تحليل الفركتالات** و**الذكاء الاصطناعي** لإنتاج **تنبؤات سعرية دقيقة** على أساس رياضي حقيقي، وليس مجرد رسم توضيحي بصري.

### المميزات الرئيسية:
- ✅ **تحديد الفركتالات**: كشف نقاط التحول الرئيسية في السعر
- ✅ **نموذج LSTM-مشابه**: شبكة عصبية مبسطة للتنبؤ
- ✅ **تحليل متعدد الأبعاد**: يدمج الاتجاه والتقلبات و RSI
- ✅ **حسابات الثقة**: نسبة الدقة لكل تنبؤ
- ✅ **الامتثال للأهداف**: يأخذ في الاعتبار TP1 و TP2 والمستويات الموجودة

---

## 🔷 المكونات الأساسية

### 1. كشف الفركتالات (Fractal Detection)

**الفركتال** هو نقطة تحول حيث:
- **Fractal High**: شمعة بأعلى أسعار أعلى من الشموع قبلها وبعدها
- **Fractal Low**: شمعة بأقل أسعار أقل من الشموع قبلها وبعدها

```
     ↓ Fractal High
   ╱─╲
  ╱   ╲
 │     │
 │     │
 └─────┘  ← يجب أن يكون أعلى من 2 شموع على كل جانب
   │ │ │
   ← depth = 2 →
```

### 2. المتوسطات المتحركة (Moving Averages)

- **MA20**: متوسط آخر 20 شمعة
- **MA50**: متوسط آخر 50 شمعة
- تحدد الاتجاه العام: `Trend = MA20 > MA50 ? UPTREND : DOWNTREND`

### 3. التقلبات (Volatility)

قياس مدى تذبذب السعر - يؤثر على حجم التنبؤات.

### 4. مؤشر RSI

يقيس قوة الاتجاه (0-100):
- RSI < 30: سوق بيع مفرط (Oversold)
- RSI > 70: سوق شراء مفرط (Overbought)

---

## 📐 الصيغ الرياضية

### أ) حساب المتوسط المتحرك (MA)

```
MA(n) = (Close[i] + Close[i-1] + ... + Close[i-n+1]) / n

حيث:
  n = عدد الفترات
  Close[i] = سعر الإغلاق في الفترة i
```

**مثال**: MA20 لآخر 20 شمعة
```
MA20 = (102 + 107 + 109 + ... ) / 20
```

---

### ب) حساب التقلبات (Volatility)

```
σ = √[ Σ(ln(P[i]/P[i-1]) - μ)² / n ]

حيث:
  P[i] = السعر في الفترة i
  ln = اللوغاريتم الطبيعي
  μ = متوسط العوائد
  n = عدد الفترات
```

**التفسير**: كلما زادت σ (سيجما)، زاد التذبذب.

---

### ج) مؤشر القوة النسبية (RSI)

```
RSI = 100 - (100 / (1 + RS))

حيث:
  RS = متوسط المكاسب / متوسط الخسائر (على آخر 14 فترة)
```

**مثال**:
```
الفترات الـ 14:
  المكاسب: +2, +3, +1, +2 ... (المتوسط = 2)
  الخسائر: -1, -1.5, -0.5 ... (المتوسط = 1)
  RS = 2 / 1 = 2
  RSI = 100 - (100 / (1 + 2)) = 100 - 33.33 = 66.67
```

---

## 🎯 خوارزمية التنبؤ

### المعادلة الرئيسية:

```
Price[t+i] = CurrentPrice + TrendComp + AttractionComp + FractalComp + OscillationComp + RSIComp

حيث:

1️⃣ TrendComponent (مكون الاتجاه):
   TrendComp = Trend × (Volatility / 2) × Strength
   
   • Trend: +1 للاتجاه الصعودي، -1 للاتجاه الهابط
   • Volatility: التقلبات الحالية
   • Strength: قوة النموذج (0.1 - 1.0)

2️⃣ AttractionComponent (الانجذاب للهدف):
   AttractionComp = ((TP2 - CurrentPrice) / ForecastLength) × (i / ForecastLength) × 0.7
   
   • يجذب السعر تدريجياً نحو الهدف الثاني
   • يزداد تأثيره مع الوقت

3️⃣ FractalComponent (مكون الفركتال):
   FractalComp = (LastFractalHigh - CurrentPrice) × 0.05 × Strength
   
   • يؤثر بناءً على آخر نقطة تحول عالية
   • يساعد في تحديد مستويات المقاومة

4️⃣ OscillationComponent (التذبذب):
   OscillationComp = sin((i / ForecastLength) × π) × Volatility × Strength × 0.5
   
   • يضيف تموج طبيعي للسعر
   • يمثل الحركة العشوائية الطبيعية

5️⃣ RSIComponent (مكون RSI):
   RSIComp = ((RSI - 50) / 50) × Volatility × 0.3
   
   • عندما RSI > 50: تأثير موجب (دعم صعود)
   • عندما RSI < 50: تأثير سالب (دعم هبوط)

```

### حساب مستوى الثقة (Confidence):

```
VolatilityConfidence = 1 - min(Volatility × 2, 1)
  ← قيم منخفضة → ثقة أعلى (سوق مستقر)

FractalConfidence = min((FractalCount / 10), 1)
  ← كل فركتال = ثقة أعلى

TrendConfidence = |Trend| × 0.8
  ← اتجاه واضح = ثقة أعلى

OverallConfidence = (Vol + Fractal + Trend) / 3
  ← متوسط الثقات الثلاثة
```

---

## 📊 خوارزمية التنبؤ خطوة بخطوة

```javascript
للفترة i من 1 إلى ForecastLength:

1. احسب TrendComponent:
   تحديد إذا كان السوق صاعد أم هابط

2. احسب AttractionComponent:
   اجذب السعر نحو TP2 بشكل تدريجي

3. احسب FractalComponent:
   احصل على آخر fractal high وأضفه للحساب

4. احسب OscillationComponent:
   أضف تموج جيبي للحركة الطبيعية

5. احسب RSIComponent:
   تأثر بموقع RSI من منتصف المقياس

6. ادمج جميع المكونات:
   Price[i] = Current + Trend + Attraction + Fractal + Oscillation + RSI

7. احسب مستوى الثقة:
   Confidence = (3 مكونات ثقة) / 3

8. خزّن النتيجة:
   { period: i, price: Price[i], confidence: Confidence }
```

---

## 🔢 مثال عملي

### البيانات المدخلة:

```
السعر الحالي: 120
TP1: 125
TP2: 130
Entry: 120
SL: 115
MA20: 119 (اتجاه صاعد)
MA50: 115
Volatility: 2.5
RSI: 65
Fractal High الأخير: 123
عدد فترات التنبؤ: 5
Strength: 0.7
```

### الحسابات:

#### للفترة 1:

```
TrendComponent = 1 × (2.5 / 2) × 0.7 = 0.875
  (اتجاه صاعد، تقلبات معتدلة، قوة متوسطة)

AttractionComponent = ((130 - 120) / 5) × (1 / 5) × 0.7 = 0.28
  (الهدف بعيد 10 نقاط، نحن في 20% من الطريق)

FractalComponent = (123 - 120) × 0.05 × 0.7 = 0.105
  (آخر قمة في 123)

OscillationComponent = sin((1/5) × π) × 2.5 × 0.7 × 0.5 = 0.43
  (تموج في البداية)

RSIComponent = ((65 - 50) / 50) × 2.5 × 0.3 = 0.225
  (RSI فوق 50 = إيجابي)

السعر المتنبأ به:
Price[1] = 120 + 0.875 + 0.28 + 0.105 + 0.43 + 0.225 = 121.915
```

#### حساب الثقة للفترة 1:

```
VolatilityConfidence = 1 - (2.5 × 2) = 1 - 5 = -4 → معايرة إلى 0
  (تقلبات عالية = ثقة منخفضة)

FractalConfidence = min((8 / 10), 1) = 0.8
  (8 فركتالات مكتشفة)

TrendConfidence = 1 × 0.8 = 0.8
  (اتجاه واضح جداً)

OverallConfidence = (0.2 + 0.8 + 0.8) / 3 = 0.6 = 60%
```

---

## 🎮 كيفية الاستخدام

### الخطوة 1: تحضير البيانات

اجمع بيانات OHLCV (Open, High, Low, Close, Volume):

```json
[
  {
    "time": 1,
    "open": 100,
    "high": 105,
    "low": 99,
    "close": 102,
    "volume": 1000
  },
  {
    "time": 2,
    "open": 102,
    "high": 108,
    "low": 101,
    "close": 107,
    "volume": 1200
  }
]
```

### الخطوة 2: إدخال المستويات

```
TP1: 125
TP2: 130
Entry: 120
SL: 115
```

### الخطوة 3: إعدادات النموذج

```
عدد الشموع المستقبلية: 20
قوة النموذج: 0.7 (متوسط)
```

### الخطوة 4: تشغيل التحليل

اضغط على **🔍 تحليل وتنبؤ**

### النتائج:

- 📊 رسم بياني يعرض: السعر التاريخي + التنبؤ + المستويات
- 📈 جدول بالتنبؤات (السعر والتغير والثقة)
- 🎯 إحصائيات النموذج (الفركتالات المكتشفة، دقة النموذج)

---

## ⚙️ معاملات النموذج

| المعامل | الوصف | القيمة الافتراضية |
|--------|--------|--------------|
| **Depth** | عمق البحث عن الفركتالات | 2 شمعة |
| **Strength** | قوة تأثير النموذج | 0.7 |
| **Forecast Length** | عدد الشموع للتنبؤ | 20 شمعة |
| **MA Period** | فترة المتوسط المتحرك | 20, 50 |
| **RSI Period** | فترة RSI | 14 شمعة |

---

## 🔍 تحسينات وملاحظات

### القوة:
- ✅ يدمج عدة مؤشرات فنية
- ✅ يأخذ بعين الاعتبار التقلبات
- ✅ يتكيف مع الأهداف المتوقعة
- ✅ يوفر مستويات ثقة لكل تنبؤ

### التحديات:
- ⚠️ التنبؤ الطويل الأمد أقل دقة (الاعتماد على بيانات أطول)
- ⚠️ الأسواق المتقلبة جداً تقلل من دقة النموذج
- ⚠️ يعتمد على الأنماط التاريخية

---

## 🚀 التطورات المستقبلية

يمكن تحسين النموذج بـ:

1. **LSTM الحقيقي**: استخدام TensorFlow.js للشبكات العصبية العميقة
2. **الفركتالات المتقدمة**: اكتشاف أنماط فركتالية معقدة
3. **التعلم التعزيزي**: تحسين معاملات النموذج تلقائياً
4. **Ensemble Models**: دمج عدة نماذج للتنبؤات
5. **بيانات السوق الحية**: التحديث الفوري للتنبؤات

---

## 📚 المراجع

- Technical Analysis: Fractals - Bill Williams
- LSTM: Understanding Long Short-Term Memory Networks
- Volatility Calculation: Historical vs Implied
- RSI: J. Welles Wilder Jr.

---

**تم التطوير بواسطة**: نموذج التنبؤ الفركتالي المتقدم v1.0
**التاريخ**: 2026
**الحالة**: نموذج عملي متقدم ✅
